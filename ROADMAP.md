# خطة بناء أريب — من الـ MVP إلى الإطلاق والتطوير

**آخر تحديث:** 28 أغسطس 2026 (مُراجَع مقابل الكود الفعلي — بعد إتمام مسار توليد وتصدير الـ PRD)
**النسخة السابقة من الخطة:** 20 أغسطس 2026

> **ملاحظة مهمة:** **المرحلة 1 اكتملت.** المسار كامل الآن من أول رسالة إلى ملف PDF حقيقي بقالب أريب: الاكتشاف → استخراج المتطلبات → مراجعتها → توليد وثيقة PRD → تصديرها. راجع القسم 1.1 لفروقات التنفيذ عن الخطة الأصلية.

---

## 0. الرؤية بإيجاز

أريب مساعد ذكاء اصطناعي يحوّل محادثة غير منظمة مع العميل إلى وثيقة متطلبات (PRD) احترافية جاهزة للتصدير — خلال دقائق بدل أيام من الاجتماعات.

**المعادلة الأساسية:** فوضى (رسائل، ملاحظات، اجتماعات) → أريب يفهم ويربط → وضوح (PRD منظّم).

---

## 1. أين وصل المشروع فعليًا (Snapshot — 28 أغسطس 2026)

| الجزء | الحالة | المكان في الكود |
|---|---|---|
| الواجهة الأمامية (React 19 + Vite 8) | ✅ جاهزة — RTL كامل، خطوط عربية محلية | [src/](src/) |
| صفحة الهبوط | ✅ جاهزة | [Landing.jsx](src/pages/Landing.jsx) |
| **حسابات المستخدمين (Auth)** | ✅ **جاهزة** — تسجيل/دخول/استعادة كلمة مرور عبر Supabase Auth | [src/features/auth/](src/features/auth/)، [AuthContext.jsx](src/contexts/AuthContext.jsx)، [ProtectedRoute.jsx](src/components/ProtectedRoute.jsx) |
| **قاعدة البيانات** | ✅ **جاهزة** — organizations / organization_members / clients / projects / messages / requirements + RLS كامل + triggers | [supabase/schema.sql](supabase/schema.sql) |
| مساحة العمل (Sidebar + قائمة المشاريع + تبويبات) | ✅ جاهزة | [AppShell.jsx](src/app/AppShell.jsx)، [Sidebar.jsx](src/components/Sidebar.jsx) |
| معالج مشروع جديد (Wizard) | ✅ جاهز — اسم / عميل / نوع / وصف | [useNewProjectFlow.js](src/features/projects/useNewProjectFlow.js) |
| **وكيل الاكتشاف الحقيقي (AI)** | ✅ **شغّال** — Gemini 3.6 Flash أساسي (مفتاحان بالتناوب) + Groq كـ fallback، مع retries وتحقق من الشكل | [server/routes/discovery.js](server/routes/discovery.js)، [server/providers/](server/providers/) |
| عقد JSON البنيوي للمحادثة | ✅ جاهز — response / intent / requirements_extracted / missing_information / contradictions / confidence / discovery_status | [discovery.system.js](server/prompts/gemini/discovery.system.js)، [validateDiscoveryResponse.js](server/lib/validateDiscoveryResponse.js) |
| حفظ المحادثة والمتطلبات | ✅ جاهز — كل رسالة في `messages`، وكل متطلب مُطبَّع في `requirements` بالدمج عبر `req_key` | [messagesService.js](src/services/messagesService.js)، [requirementsService.js](src/services/requirementsService.js) |
| شاشة مراجعة المتطلبات (تعديل/إضافة/حذف) | ✅ جاهزة — 7 أنواع، أولويات MoSCoW، fallback على `discovery_state` | [RequirementsReview.jsx](src/features/projects/RequirementsReview.jsx) |
| قالب الـ PRD PDF | ✅ جاهز — 8 صفحات، عربي RTL حقيقي، خطوط مضمّنة، نص قابل للبحث (مُتحقَّق منه على مخرجات حقيقية) | [src/templates/areep/](src/templates/areep/) |
| **توليد PRD حقيقي** | ✅ **شغّال** — Groq أساسي ← Gemini fallback، نفس pipeline الاكتشاف (retries + تحقق من الشكل) | [server/routes/prd.js](server/routes/prd.js)، [prd.system.js](server/prompts/groq/prd.system.js) |
| **تصدير الوثيقة بقالب أريب** | ✅ **شغّال** — من الشات مباشرة أول ما يخلص أريب فهم الفكرة، ومن صفحة المتطلبات، بنفس المسار المشترك | [usePrdGeneration.js](src/features/projects/usePrdGeneration.js) |
| معاينة الوثيقة (3 لوحات + فهرس) | ✅ جاهزة — الـ PDF الحقيقي مضمّن حيًّا، لا mockup | [PrdPreview.jsx](src/features/projects/PrdPreview.jsx) |
| تقدّم حالة المشروع (`status`) | ✅ يتحرّك — `discovery` ← `ready_for_review` ← `prd_generated` | [projectsService.js](src/services/projectsService.js) |
| حفظ الوثيقة المُولَّدة | ⚠️ جزئي — نسخة واحدة في `projects.prd_data`، بلا تاريخ إصدارات ولا Storage | [schema.sql](supabase/schema.sql) |
| تصدير Markdown | ✅ جاهز — من نفس الـ JSON، بلا استدعاء ثانٍ للنموذج | [prdMarkdown.js](src/lib/prdMarkdown.js) |
| تصدير DOCX | ❌ غير موجود | — |
| لوحة مشاريع مستقلة (Dashboard) | ⚠️ جزئي — القائمة الجانبية تقوم بالدور، بلا فلاتر ولا روابط تنزيل | [Sidebar.jsx](src/components/Sidebar.jsx) |
| حماية الـ API الخلفي | ❌ `/api/discovery` مفتوح بلا مصادقة | [server/routes/discovery.js](server/routes/discovery.js) |
| حدود الاستخدام / الاشتراكات / الدفع | ❌ غير موجود | — |
| النشر (Deployment) | ❌ الواجهة والخادم محليان فقط (`localhost:3002`) | [discoveryService.js](src/services/discoveryService.js) |

### 1.1 ما اختلف عن الخطة السابقة (قرارات تنفيذية مُتّخذة)

| في الخطة القديمة | ما تم فعليًا | السبب |
|---|---|---|
| توليد PDF عبر `build_prd.py` + Playwright/Chromium (HTML→PDF) | `@react-pdf/renderer` داخل المتصفح مباشرة | لا حاجة لـ Python ولا Chromium على الخادم؛ نص قابل للتحديد والبحث، ملف أخف، ودعم RTF/العربي مُتحقَّق منه فعليًا. (السبب موثّق في رأس [prdPdf.jsx](src/templates/areep/prdPdf.jsx)) |
| علامة نصية داخلية `[READY_FOR_PRD]` يخفيها الفرونت | حقلان بنيويان في الـ JSON: `confidence` (0-100) و `discovery_status` (`in_progress`/`ready`) | أنظف وأدق من تحليل نص، ويعطي مؤشر تقدّم رقمي بدل إشارة ثنائية |
| Claude Sonnet لتوليد الـ PRD | Groq (`openai/gpt-oss-120b`) هو الخيار المُخطط له حاليًا | تكلفة صفرية أثناء التطوير؛ يظل قابلًا للاستبدال بـ Claude لاحقًا (نفس الواجهة `generateJSON`) |
| Auth وقاعدة البيانات في المرحلة 2 | أُنجزت مبكرًا قبل ربط الـ PDF | بُنيت مساحة العمل كاملة أولًا، فصار الحفظ شرطًا لكل شيء |
| قسمة النماذج: محادثة رخيصة + توليد PRD مكلف | ✅ مطبّقة كما هي | — |

---

## 2. المرحلة 1 — الـ MVP الوظيفي ✅ مكتملة

**الهدف (تحقّق):** مستخدم يفتح الموقع، يحكي عن مشروعه، يراجع المتطلبات، ويطلع بـ PDF حقيقي عن مشروعه هو.

### 2.1 توليد PRD حقيقي ✅
- `POST /api/prd` على نمط `/api/discovery` نفسه: نفس pipeline الـ `withRetries` + `stripJsonFences` + validator خاص ([validatePrdResponse.js](server/lib/validatePrdResponse.js)).
- **Groq أساسي، Gemini fallback** — عكس مسار المحادثة عمدًا: توليد الـ PRD ما له متطلب لهجة أو شخصية، فأي مزوّد يقدر يحترم نفس عقد الـ JSON.
- [prd.system.js](server/prompts/groq/prd.system.js) يستقبل المتطلبات المُطبَّعة مجمّعة حسب النوع (لا يرى المحادثة الخام إطلاقًا) ويُخرج المخطط الكامل، مع قاعدة "ممنوع اختلاق معلومات" مكرّرة صراحة.
- [prdMapper.js](src/lib/prdMapper.js) يحوّل ذلك إلى الشكل الذي يقرأه القالب ([prdPdf.jsx](src/templates/areep/prdPdf.jsx)) — دفاعي بالكامل، كل حقل له بديل عربي صادق، ولا يرمي استثناء على أي مفتاح ناقص.

### 2.2 التصدير بقالب أريب ✅
- **من الشات مباشرة:** أول ما يرجع وكيل الاكتشاف `discovery_status: "ready"`، تظهر بطاقة "أريب فهم فكرتك" ومعها زر **جهّز وثيقة PRD** — ضغطة واحدة تولّد الوثيقة، تحفظها، تبني الـ PDF بقالب أريب، **تنزّل الملف فورًا**، ثم تنقلك للمعاينة.
- **من صفحة المتطلبات:** نفس المسار بالضبط عبر [usePrdGeneration.js](src/features/projects/usePrdGeneration.js) المشترك — الفارق الوحيد أنها تمرّر المتطلبات المحمّلة عندها أصلًا بدل قراءتها من جديد.
- المصدر يتدرّج: جدول `requirements` أولًا، ثم لقطة `discovery_state` إن لم يكن الجدول مهاجَرًا — فالتصدير ما يفشل لأن المهاجرة ما انشغّلت.

### 2.3 تقدّم حالة المشروع ✅
`discovery` ← `ready_for_review` (حين يقول أريب إنه فهم) ← `prd_generated` (بعد التوليد). الترقية أحادية الاتجاه: مشروع وصل `prd_generated` ما يرجع للخلف لو كمّل المستخدم الدردشة بعدها.

### 2.4 ما تبقّى من هذه المرحلة (مؤجَّل عن قصد)
- **تاريخ إصدارات الوثائق**: حاليًا `projects.prd_data` يحمل **نسخة واحدة** فقط — إعادة التوليد تستبدل السابقة. جدول `generated_documents` (project_id, format, prd_json, storage_path, created_at) + Supabase Storage هو ما يحوّل هذا إلى تاريخ حقيقي مع روابط تنزيل دائمة.
- **تعديل / إعادة توليد قسم** داخل المعاينة — معروضان ومعطّلان بوسم "قريبًا"، لا مُزيَّفان.

**مخرج المرحلة 1:** ✅ منتج يشتغل فعليًا من أول رسالة إلى PDF حقيقي بقالب أريب.

---

## 3. المرحلة 2 — التجهيز للإطلاق

### 3.1 حماية الخادم — 🔴 أهم بند متبقٍّ قبل أي نشر
`/api/discovery` و`/api/prd` كلاهما مفتوح للجميع بلا مصادقة: أي شخص يعرف الرابط يستهلك مفاتيح Gemini/Groq. المطلوب: تمرير Supabase JWT من الواجهة والتحقق منه في Express middleware واحد قبل أي استدعاء لمزوّد.

### 3.2 لوحة مشاريع حقيقية
القائمة الجانبية تكفي مؤقتًا، لكن الناقص: صفحة مشاريع مستقلة مع فلترة بالحالة، بحث، وزر تنزيل آخر PDF لكل مشروع (يعتمد على تاريخ الوثائق في 2.4).

### 3.3 صيغ تصدير إضافية
Markdown ✅ مُنجز. المتبقي DOCX — **من نفس الـ JSON** المخزَّن، بلا إعادة استدعاء للنموذج لكل صيغة (وهذا بالضبط سبب تخزين الـ JSON الخام لا الـ PDF).

### 3.4 حدود الاستخدام (Rate Limiting)
- النسخة المجانية: مشروعان بالشهر.
- قيد واقعي إضافي **الآن**: مفاتيح Gemini المجانية محدودة بـ 20 طلب/يوم لكل مفتاح (مفتاحان = 40 طلب/يوم إجمالًا للتطوير كله). هذا سقف تجريبي، لا يكفي لأي إطلاق — يحتاج ترقية للخطة المدفوعة أو تحويل الحمل الأساسي لـ Groq قبل فتح الباب لمستخدمين حقيقيين.

### 3.5 النشر
- الواجهة: Vercel/Netlify.
- الخادم: خدمة Node (Railway/Render) أو Supabase Edge Functions، مع ضبط `VITE_AREEP_API_URL` بدل `localhost:3002`.
- تنظيف: [README.md](README.md) لا يزال قالب Vite الافتراضي — يحتاج توثيق تشغيل حقيقي (`npm run dev` + `npm run server` + خطوة تشغيل `schema.sql`).

### 3.6 اختبار حقيقي (Critical)
- 5-10 مستخدمين فعليين (مستقلّون، أصحاب مشاريع صغيرة).
- راقب: هل تتوقف الأسئلة عند النقطة الصحيحة؟ متى يصل `confidence` لـ 85+ — مبكرًا أم متأخرًا؟ هل الـ PDF يُقنع مستخدمًا حقيقيًا؟
- عدّل الـ System Prompt بناءً على نتائج حقيقية، لا تخمين.

**مخرج المرحلة 2:** منتج منشور وآمن، جاهز لعدد محدود من المستخدمين الحقيقيين (Beta مغلق).

---

## 4. المرحلة 3 — الإطلاق (Soft Launch)

### 4.1 استراتيجية الإطلاق
- Beta مغلق (رابط دعوة) لـ 20-50 مستخدم من الشريحة المستهدفة (مستقلّون، شركات برمجة صغيرة).
- نموذج قصير بعد كل PRD مُولَّد: "هل هذا يمثّل مشروعك بدقة؟"

### 4.2 قنوات أولية
- مجتمعات مطورين ومستقلّين عرب (X، تليجرام/ديسكورد متخصصة).
- محتوى "قبل/بعد" يوضّح التحوّل (فوضى محادثة → PRD منظّم) — أقوى دليل بصري على قيمة المنتج، والقالب الحالي يصلح للعرض المباشر.

### 4.3 مراقبة التكلفة والأخطاء
- تتبّع استهلاك التوكينز لكل نموذج (محادثة مقابل توليد PRD) من أول يوم.
- تسجيل أخطاء حقيقي بدل `console.warn` فقط في [discovery.js](server/routes/discovery.js) — كم مرة يسقط Gemini ويشتغل Groq فعلًا؟

**مخرج المرحلة 3:** إطلاق علني محدود، وبيانات استخدام حقيقية أولى.

---

## 5. المرحلة 4 — ما بعد الإطلاق والنمو

### 5.1 نموذج الربح
- خطة Pro (مشاريع غير محدودة + تصدير احترافي) وAgency (White Label — البنية جاهزة له: `organizations` + `organization_members` موجودان في المخطط من اليوم الأول).
- بوابة دفع (Stripe أو Moyasar للسوق السعودي).

### 5.2 خصائص لاحقة (حسب الأولوية)
1. **تكامل Jira / ClickUp** — تصدير User Stories مباشرة كـ tickets.
2. **تقدير التكلفة والوقت التلقائي** بناءً على نطاق المشروع.
3. **مخططات UML / Wireframes أولية**.
4. **دعم فرق العمل** — أكثر من عضو بنفس المشروع (جدول `organization_members` يدعم `admin`/`member` أصلًا، تنقصه واجهة ودعوات).

### 5.3 مؤشرات النجاح
- عدد المشاريع المُنشأة شهريًا.
- نسبة إكمال جلسة الاكتشاف (بدون هجر في منتصف الطريق) — قابلة للقياس فورًا عبر `confidence` المحفوظ لكل مشروع.
- الوقت من أول رسالة إلى PRD جاهز (الهدف: 15 دقيقة).
- معدل التحويل من مجاني إلى مدفوع.

---

## 6. الملخص التقني (Stack الفعلي)

| الطبقة | التقنية |
|---|---|
| الواجهة الأمامية | React 19 + Vite 8 + React Router 7 + CSS يدوي (RTL بخصائص منطقية) |
| محادثة أريب | Gemini 3.6 Flash (مفتاحان بالتناوب) ← Groq `openai/gpt-oss-120b` كـ fallback |
| توليد PRD | Groq `openai/gpt-oss-120b` ← Gemini fallback + `@react-pdf/renderer` للإخراج |
| الخادم | Express 5 (Node، منفذ 3002) — proxy يحمل المفاتيح |
| قاعدة البيانات | Supabase (Postgres + Auth + RLS)، Storage لاحقًا |
| الاستضافة | لم تُنشر بعد |

---

## 7. المخاطر والاعتبارات

- **جودة الاستخراج التلقائي**: مُعالَجة جزئيًا — شاشة مراجعة المتطلبات تسمح بالتعديل/الحذف/الإضافة قبل التصدير، وقاعدة "ممنوع اختلاق معلومات" مضمّنة في الـ prompt.
- **حدود المفاتيح المجانية**: 20 طلب/يوم لكل مفتاح Gemini — أشد قيد عملي حاليًا، ويجب حلّه قبل المرحلة 3.
- **الاعتماد على مزوّد واحد**: محلول لمسار المحادثة (Gemini→Groq)، ويجب تكرار نفس النمط لمسار توليد الـ PRD منذ بنائه.
- **خادم مكشوف**: راجع 3.1 — لا نشر قبل إغلاقه.
- **لا اختبارات آلية إطلاقًا**: أي تغيير على عقد الـ JSON أو الـ RLS يُكتشف يدويًا فقط. الحد الأدنى المقترح: اختبارات وحدة للـ validators، و`stripJsonFences`، و`prdMapper` (هذا الأخير تحديدًا — هو الطبقة التي إذا انزلقت بصمت خرجت الوثيقة ناقصة صفحة كاملة دون أي خطأ ظاهر، وقد حصل فعلًا: صفحة الأهداف كانت تخرج فارغة لأن المعاينة كانت تمرّر `{}` بدل المتطلبات).

---

## 8. الترتيب الزمني المُحدَّث

| البند | المدة التقديرية |
|---|---|
| ~~المرحلة 1 (توليد PRD + الحالة + التصدير)~~ | ✅ مكتملة |
| تاريخ إصدارات الوثائق (`generated_documents` + Storage) | 1-2 يوم |
| المرحلة 2 (حماية + نشر + تصدير + لوحة) | 1.5-2 أسبوع |
| المرحلة 3 — Soft Launch | أسبوع إطلاق + 3-4 أسابيع مراقبة |
| المرحلة 4 — نمو مستمر | مستمر بعد الإطلاق |

تقديرات لفريق صغير (1-2 مطور) يشتغل بشكل مركّز — تتغيّر حسب الوقت الفعلي المتاح.
