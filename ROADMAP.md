# خطة بناء أريب — من الـ MVP إلى الإطلاق والتطوير

**آخر تحديث:** 1 سبتمبر 2026 (مُراجَع مقابل الكود والمشروع المنشور فعليًا)
**النسخة السابقة:** 28 أغسطس 2026

> **الحالة:** المرحلتان 1 و2 اكتملتا. المنتج **منشور ويعمل** على
> [areep.netlify.app](https://areep.netlify.app)، بمسار كامل من أول رسالة إلى وثيقة PRD مُصدَّرة، وبواجهة
> برمجية محمية خلف جلسة مستخدم حقيقية. المتبقي هو المرحلة 3: الاختبار مع مستخدمين حقيقيين، وما يسبقه من
> حدود استخدام وتاريخ للوثائق.

---

## 0. الرؤية بإيجاز

أريب مساعد ذكاء اصطناعي يحوّل محادثة غير منظمة مع العميل إلى وثيقة متطلبات (PRD) احترافية جاهزة للتصدير —
خلال دقائق بدل أيام من الاجتماعات.

**المعادلة:** فوضى (رسائل، ملاحظات، اجتماعات) → أريب يفهم ويربط → وضوح (PRD منظّم).

---

## 1. أين وصل المشروع فعليًا

| الجزء | الحالة | المكان |
|---|---|---|
| **النشر** | ✅ منشور على Netlify مع SPA fallback ومتغيرات بيئة | [netlify.toml](netlify.toml)، [public/_redirects](public/_redirects) |
| **الخادم الخلفي للذكاء الاصطناعي** | ✅ Supabase Edge Functions — المفاتيح في Supabase Secrets، المتصفح ما يشوفها أبدًا | [supabase/functions/](supabase/functions/) |
| **حماية الواجهة البرمجية** | ✅ تتطلب جلسة مستخدم حقيقية، لا مجرد مفتاح صالح | [_shared/auth.ts](supabase/functions/_shared/auth.ts) |
| وكيل الاكتشاف | ✅ Gemini أساسي ← Groq بديل، مع retries وتحقق من شكل الرد | [functions/discovery](supabase/functions/discovery/index.ts) |
| توليد الـ PRD | ✅ Groq أساسي ← Gemini بديل، بنفس المسار | [functions/prd](supabase/functions/prd/index.ts) |
| قاعدة البيانات + RLS | ✅ organizations / members / clients / projects / messages / requirements | [schema.sql](supabase/schema.sql) |
| حسابات المستخدمين | ✅ بريد + كلمة مرور، **ودخول عبر Google** (مفعّل ومتحقَّق منه) | [useAuth.js](src/hooks/useAuth.js) |
| محرّك المتطلبات + شاشة المراجعة | ✅ سبعة أنواع بمعرّفات ثابتة، تعديل/حذف/إضافة قبل التوليد | [RequirementsReview.jsx](src/features/projects/RequirementsReview.jsx) |
| قالب الـ PRD وتصديره | ✅ ثمان صفحات RTL أصيلة، PDF و Markdown من نفس المصدر | [src/templates/areep/](src/templates/areep/) |
| **صفحة الهبوط** | ✅ أُعيد بناؤها بالكامل — تجربة تحريرية RTL بخمسة فصول وعرض حي | [src/pages/Landing.jsx](src/pages/Landing.jsx) |
| الصفحات العامة والقانونية | ✅ الأسئلة الشائعة، الخصوصية، الشروط، صفحة 404 | [src/pages/](src/pages/) |
| SEO | ✅ عناوين وأوصاف وcanonical وOG، وsitemap.xml وrobots.txt من مصدر واحد | [routes.js](src/lib/routes.js)، [vite/seoPlugin.js](vite/seoPlugin.js) |
| الهوية البصرية | ✅ أربع نسخ من الشعار بقاعدة فاتح/غامق، وأيقونات مولّدة من العلامة الحقيقية | [src/lib/brand.js](src/lib/brand.js) |
| التايبوغرافي | ✅ ثمانية سيريف ديسبلاي للعناوين + IBM Plex Sans Arabic للنصوص | [src/index.css](src/index.css) |
| التحليلات | ⚠️ واجهة أحداث جاهزة بلا مزوّد — لا تعمل حتى يُضبط `VITE_ANALYTICS_URL` | [analytics.js](src/lib/analytics.js) |
| تاريخ الوثائق | ❌ نسخة واحدة في `projects.prd_data` — إعادة التوليد تستبدل السابقة | — |
| تصدير DOCX | ❌ غير موجود | — |
| لوحة مشاريع مستقلة | ❌ القائمة الجانبية تقوم بالدور، بلا فلاتر ولا بحث | — |
| حدود استخدام لكل مستخدم | ❌ غير موجود | — |
| اختبارات آلية | ❌ غير موجودة | — |

---

## 2. المرحلة 1 — الـ MVP الوظيفي ✅ مكتملة

المسار كامل: جلسة اكتشاف بالعربية → استخراج المتطلبات ومراجعتها → توليد وثيقة PRD → تصديرها PDF أو
Markdown. حالة المشروع تتحرك `discovery` ← `ready_for_review` ← `prd_generated` بترقية أحادية الاتجاه.

---

## 3. المرحلة 2 — التجهيز للإطلاق ✅ مكتملة

### 3.1 نقل الخادم إلى Edge Functions ✅
كان الـ MVP يعتمد على خادم Express محلي غير منشور، ومفاتيح Gemini وGroq فيه. الآن الطرفان (`discovery`
و`prd`) على Supabase Edge Functions، والمفاتيح في Supabase Secrets — المتصفح ما يشوف مفتاح مزوّد أبدًا.

### 3.2 إغلاق الثغرة الأمنية ✅
كانت أخطر نقطة في الخطة السابقة: `/api/discovery` مفتوح لأي أحد يعرف الرابط. وأثناء النقل تبيّن أن
`verify_jwt` وحده **لا يكفي** — المفتاح العام (`sb_publishable_…`) المشحون داخل الواجهة يمرّ منه ويصل
للدالة (مُتحقَّق منه على النسخة المنشورة). فأُضيف فحص فوقه يشترط رمز جلسة حقيقيًا بـ`role=authenticated`
و`sub`، وهذا ما يغلق باب استهلاك الحصة فعليًا.

### 3.3 النشر ✅
منشور على Netlify مع إعادة توجيه SPA (بدونها كل مسار غير `/` كان يرجع 404 من Netlify نفسه).

### 3.4 تسجيل الدخول عبر Google ✅
مفعّل ومتحقَّق منه من البداية للنهاية حتى شاشة قوقل.

### 3.5 صفحة الهبوط والواجهة العامة ✅
أُعيد بناء صفحة الهبوط من الصفر: حقل جزيئات يتجمّع في شكل علامة أريب، خمسة فصول مثبّتة بالتمرير، عرض حي
لجلسة حقيقية، وثيقة تُبنى أمام الزائر، ومزايا بأدلة من مخرجات المنتج نفسه. ومعها الصفحات القانونية
والأسئلة الشائعة وصفحة 404 وطبقة SEO كاملة.

**مُتحقَّق منه:** 13 مقاسًا من 2940×1758 إلى 320×700 بلا أي تجاوز أفقي، 60 إطارًا/ثانية أثناء التمرير،
صفر مهام طويلة، و**CLS** بين 0.008 و0.023.

---

## 4. المرحلة 3 — الإطلاق (Soft Launch) 🔜 التالية

### 4.1 ما يجب إنجازه *قبل* دعوة مستخدمين

**أ. حدود استخدام لكل مستخدم — 🔴 الأولوية الأولى**
الواجهة محمية بجلسة، لكن **مستخدمًا واحدًا مسجَّلًا يقدر يستنزف حصة المشروع كاملة**. يلزم عدّاد لكل
مستخدم/شهر داخل الدالتين قبل أي دعوة عامة.

**ب. حدود المزوّدين المجانيين — 🔴 قيد فعلي**
مفاتيح Gemini المجانية محدودة بـ 20 طلبًا/يوم لكل مفتاح (مفتاحان = 40 يوميًا). هذا سقف تجريبي لا يكفي
لأي إطلاق. الحل: ترقية الخطة، أو تحويل الحمل الأساسي لـ Groq.

**ج. تاريخ الوثائق**
`projects.prd_data` يحمل نسخة واحدة فقط. جدول `generated_documents` + Supabase Storage يحوّله إلى تاريخ
حقيقي بروابط تنزيل دائمة — وهو شرط لأي «آخر وثيقة» في لوحة المشاريع.

### 4.2 الإطلاق نفسه
- Beta مغلق برابط دعوة لـ 20-50 مستخدمًا من الشريحة المستهدفة (مستقلّون، شركات برمجة صغيرة).
- نموذج قصير بعد كل PRD: «هل هذا يمثّل مشروعك بدقة؟»
- محتوى «قبل/بعد» يوضّح التحوّل — أقوى دليل بصري على القيمة.

### 4.3 المراقبة
- تفعيل مزوّد للتحليلات (`VITE_ANALYTICS_URL`) — الواجهة جاهزة والأحداث مكتوبة، ينقصها المزوّد فقط.
- تتبّع استهلاك التوكينز لكل مسار، وكم مرة يسقط المزوّد الأساسي ويتولّى البديل.

---

## 5. المرحلة 4 — ما بعد الإطلاق

### 5.1 نموذج الربح
خطة Pro (مشاريع غير محدودة + تصدير احترافي) وAgency (White Label). البنية جاهزة: `organizations` و
`organization_members` موجودان من اليوم الأول. ثم بوابة دفع (Stripe أو Moyasar).

### 5.2 خصائص لاحقة
1. **لوحة مشاريع مستقلة** — فلترة بالحالة، بحث، تنزيل آخر PDF (تعتمد على 4.1-ج).
2. **تصدير DOCX** — من نفس الـ JSON المخزَّن، بلا إعادة استدعاء للنموذج.
3. **تكامل Jira / ClickUp** — تصدير قصص المستخدم كـ tickets.
4. **تقدير التكلفة والوقت** بناءً على نطاق المشروع.
5. **دعم فرق العمل** — الجدول يدعم `admin`/`member` أصلًا، تنقصه واجهة ودعوات.

### 5.3 مؤشرات النجاح
- عدد المشاريع المُنشأة شهريًا.
- نسبة إكمال جلسة الاكتشاف — قابلة للقياس فورًا عبر `confidence` المحفوظ لكل مشروع.
- الوقت من أول رسالة إلى PRD جاهز (الهدف: 15 دقيقة).
- معدل التحويل من مجاني إلى مدفوع.

---

## 6. الملخص التقني

| الطبقة | التقنية |
|---|---|
| الواجهة | React 19 + Vite 8 + React Router 7، CSS يدوي بتوكِنات، RTL أصيل |
| الخطوط | ثمانية سيريف ديسبلاي (العناوين) + IBM Plex Sans Arabic (النصوص)، مستضافة ذاتيًا |
| الخادم | Supabase Edge Functions (Deno) — `discovery` و`prd` |
| النماذج | محادثة: Gemini ← Groq · وثيقة: Groq ← Gemini |
| قاعدة البيانات | Supabase Postgres + Auth + RLS |
| توليد الوثيقة | `@react-pdf/renderer` في المتصفح، مفصول عن حزمة الزوّار |
| الاستضافة | Netlify |

---

## 7. المخاطر والاعتبارات

- **استنزاف الحصة من مستخدم مسجَّل** — أخطر ما تبقّى. راجع 4.1-أ.
- **حدود المزوّدين المجانيين** — 40 طلبًا يوميًا سقفٌ لا يحتمل إطلاقًا. راجع 4.1-ب.
- **خادم Express القديم لا يزال في المستودع** — `server/` يعمل كمسار تراجع عبر `VITE_AREEP_API_URL`
  (وهو مُطفأ في الإنتاج). يجب حذفه بعد أن تثبت Edge Functions استقرارها، وإلا بقيت نسختان من نفس
  التعليمات تتباعدان بصمت.
- **علامة أريب لا تظهر في شاشة قوقل** — تعرض `…supabase.co` لأن قوقل يعرض مضيف رابط العودة، ولا يستبدله
  بالاسم إلا لتطبيق موثّق على دومين تملكه. يحتاج شراء دومين + إضافة Custom Domain في Supabase.
- **جودة الاستخراج التلقائي** — مُعالَجة بشاشة المراجعة الإلزامية قبل التوليد، وبقاعدة «ممنوع اختلاق
  معلومات» في تعليمات النموذج.
- **لا اختبارات آلية إطلاقًا** — أي تغيير على عقد الـ JSON أو الـ RLS يُكتشف يدويًا فقط. الحد الأدنى
  المقترح: اختبارات وحدة للـ validators، و`stripJsonFences`، و`prdMapper` (هذا الأخير تحديدًا — طبقة إذا
  انزلقت بصمت خرجت الوثيقة ناقصة صفحة كاملة دون أي خطأ ظاهر، وقد حصل فعلًا).

---

## 8. الترتيب الزمني المُحدَّث

| البند | المدة التقديرية |
|---|---|
| ~~المرحلة 1 — الـ MVP الوظيفي~~ | ✅ مكتملة |
| ~~المرحلة 2 — التجهيز والنشر~~ | ✅ مكتملة |
| حدود الاستخدام + تاريخ الوثائق (قبل الدعوات) | 3-5 أيام |
| المرحلة 3 — Soft Launch | أسبوع إطلاق + 3-4 أسابيع مراقبة |
| المرحلة 4 — نمو مستمر | مستمر بعد الإطلاق |

تقديرات لفريق صغير (1-2 مطور) يشتغل بشكل مركّز — تتغيّر حسب الوقت الفعلي المتاح.
